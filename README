Сборка source distribution и всех образов docker для тестирования:

	gradle buildDocker

Сборка source distribution и конкретного образа:

	gradle buildDockerWeb

	где имя задачи соответсвует имени файла в ./docker/, например, при наличии файла docker/DockerFilePop будет сгенерирована задача buildDockerPop, в которой соберется образ с тегом {dockerRegistry}/taskexecutor-pop:my_git_branch

В случае, если сборка происходит в ветке master, source distribution будет загружен на pypiIntrUploadUrl с авторизацией по pypiIntrUsername:pypiIntrPassword
dockerRegistry, pypiIntrUploadUrl, pypiIntrUsername и pypiIntrPassword определены в файле gradle.properties


Установка: 

	virtualenv -p `which python3` /usr/local/te && . /usr/local/te/bin/activate && pip install -U --extra-index-url http://repo.intr:8181/ taskexecutor
